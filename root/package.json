{
  "name": "<%= S(name).slugify().value() %>",
  "description": "<%= description %>",
  "version": "0.0.1",
  "author": "<%= author %>",
  "dependencies": {
    "@adamkiss/spike-frank": "~0.4.8",
    "@adamkiss/webpack-caddy-proxy": "~0.1.0",
    "bootstrap": "~4.0.0",
    "include-media-mq": "~1.0.0",
    "node-sass": "~4.8.3",
    "sass-loader": "~6.0.7",
    "spike": "github:adamkiss/spike",
    "spike-optimize": "^0.1.2",
    "spike-records": "~2.2.0",
    "webpack": "^3.8.1"
  },
  "devDependencies": {
    "eslint": "~4.19.1",
    "xo": "~0.20.3"
  },
  "main": "app.js",
  "private": true,
  "scripts": {
    "deploy": "rsync -vurz -e 'ssh -p 22' --exclude='.*.php' --include='include' --include='include/**' --include='public' --include='public/**' --exclude='*' --exclude='include/**/.*' ./ <%= server %>",
    "deploy-dry": "rsync --dry-run -vvurz -e 'ssh -p 22'  --exclude='.*.php' --include='include' --include='include/**' --include='public' --include='public/**' --exclude='*' ./ <%= server %>",
    "prod": "NODE_ENV=production spike clean && spike compile -e production && npm run deploy",
    "prod-dry": "NODE_ENV=production spike clean && spike compile -e production && npm run deploy-dry"
  },
  "xo": {
    "semicolon": false,
    "rules": {
      "generator-star-spacing": 0,
      "capitalized-comments": 0,
      "require-yield": 0
    }
  }
}
